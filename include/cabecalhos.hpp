#ifndef _CABECALHOS_HPP_
#define _CABECALHOS_HPP_

#include <iostream>
#include <string>
#include <vector>
#include <windows.h>
#include <random>
#include <fstream>
#include <algorithm>
#include <sstream>
#include <cmath>
#include <ctime>
#include <locale.h>

using namespace std;

// Numeração para guiar o usuário no menu dos jogos
const int velha = 3;
const int reversi = 4;
const int connect4 = 5;
const int sair = 1;
const int voltar = 2;
const int contra_pessoa = 1;
const int contra_computador = 2;

string opcao_invalida = "ERRO: op��o inv�lida. Escolha novamente: ";

#endif
